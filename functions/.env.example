# Copy file này thành ".env" (bỏ ".example") và điền token thật.
# .env KHÔNG được commit lên Git — thêm vào .gitignore.
# Lấy TELEGRAM_BOT_TOKEN từ @BotFather trên Telegram (lệnh /mybots > chọn bot > API Token).
TELEGRAM_BOT_TOKEN=123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
